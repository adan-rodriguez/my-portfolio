---
import Layout from "../layouts/Layout.astro";
import HomeIcon from "../ui/icons/HomeIcon.astro";
---

<Layout
  title="Página no encontrada - Adán Rodríguez - Desarrollador Web"
  description="¡Hola! Lo siento, pero no pude encontrar lo que buscabas. Verifica que la dirección URL sea correcta."
>
  <link
    rel="preload"
    href="/images/travolta-not-found.gif"
    as="image"
    type="image/gif"
    slot="preloads"
  />

  <style is:inline slot="critical-css">
    ._404 {
      padding-block: 2rem;

      ._404-container {
        @media (width >= 768px) {
          display: flex;
          gap: 2rem;
          align-items: center;
        }
      }

      .travolta {
        width: 100%;
        max-width: 27rem;
        margin-inline: auto;
        mask-image: linear-gradient(to bottom, black 80%, transparent 100%);
      }

      .text {
        max-width: 65ch;

        h2 {
          width: fit-content;
          font-weight: 900;
          font-size: 2rem;
          background-image: linear-gradient(
            to right,
            var(--color-primary),
            var(--text-color-primary)
          );
          color: transparent;
          -webkit-background-clip: text;
          margin-block: 1rem;
        }

        p {
          text-wrap: pretty;
          margin-bottom: 1rem;
        }

        a {
          display: flex;
          width: fit-content;
          align-items: center;
          column-gap: 0.25rem;
          padding: 0.25rem 1.25rem;
          border-radius: 0.25rem;
          background-image: linear-gradient(
            to bottom right,
            black,
            var(--color-secondary),
            black
          );
          color: rgb(248 250 252);
          box-shadow: var(--shadow-black-lg);
          transition: scale 0.3s;
          font-size: 0.875rem;

          &:is(:hover, :focus-visible) {
            background-image: linear-gradient(
              to bottom right,
              black,
              var(--color-primary),
              black
            );
            scale: 1.05;
          }

          &:active {
            translate: 0 0.125rem;
          }
        }
      }
    }
  </style>

  <section class="_404">
    <div class="_404-container">
      <img
        src="/images/travolta-not-found.gif"
        alt="John Travolta haciendo gestos de que no encuentra a nadie en una escena de Pulp Fiction"
        class="travolta"
      />
      <div class="text">
        <h2>Página no encontrada</h2>
        <p>
          ¡Hola! Lo siento, pero no pude encontrar lo que buscabas. Verifica que
          la dirección URL sea correcta.
        </p>
        <a href="/"><HomeIcon /> Ir al inicio</a>
      </div>
    </div>
  </section>
</Layout>
